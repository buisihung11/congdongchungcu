export type TBrand = {
  id: number;
  name: string;
  // active: number;
  iconUrl: string;
  imageUrl: string;
  segmentId: number;
  segmentName: string;
};

export type TBrands = {
  id: number;
  name: string;
  // active: number;
  iconUrl: string;
  imageUrl: string;
  segmentId: number;
  segmentName: string;
};
